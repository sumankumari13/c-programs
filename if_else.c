#include <stdio.h>

int main()
{
    int age;
    printf("Enter the age\n");
    scanf("%d", &age);

    // if (age != 90)
    if (age >= 90)
    {
        printf("You can drive");
    }
    else
    {
        printf("You cannot drive\n");
    }

    if (age = 50)
    {
        /* code */
        printf(" half century");
    }

    return 0;
}
